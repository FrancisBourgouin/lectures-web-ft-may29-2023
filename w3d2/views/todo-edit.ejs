<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Todo List Of Destiny</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body>
    <header>
      <h1>Todo List Of Destiny YEAH</h1>
    </header>
    <main>
      <h1>Edit the task: <%= todo.task %></h1>
      <form action="/todos/<%= todo.id %>" method="POST">
        <div>
          <label for="">Task name</label>
          <input name="task" type="text" value="<%= todo.task %>" />
        </div>
        <div>
          <label for="">Description</label>
          <input name="description" type="text" value="<%= todo.description %>" />
        </div>
        <div>
          <label for="">Is completed</label>
          <input name="isComplete" type="checkbox" checked="<%= todo.isComplete %>" />
        </div>
        <div>
          <button>Update the todo!</button>
        </div>
      </form>

      <a href="/todos">Go back to all the todos</a>
    </main>
  </body>
</html>
